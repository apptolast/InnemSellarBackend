apiVersion: v1
kind: Service
metadata:
  name: postgres-external
  namespace: apptolast-inemsellar
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
    - name: postgresql
      port: 5432
      targetPort: 5432
      nodePort: 30435
      protocol: TCP
  selector:
    app: postgres
